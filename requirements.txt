# Python 3.14 is what production runs on (see Dependencies/log_files). 3.11+ should work,
# but note: algo_vpin_v2/data_feed.py and skills/price_action_skill.py use `Any` without
# importing it, which only Python 3.14's lazy annotations tolerate. Use 3.14 to be safe.

# Broker (Dhan) - Tradehull handles PIN+TOTP login and creates the Dependencies/ folder
dhanhq>=2.2.0
Dhan-Tradehull>=3.3.2

# Core numerics / ML
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0
xgboost>=2.0.0
arch>=8.0.0
joblib>=1.3.0
pytz>=2024.1

# Vision CNN brain (inference runs on every bar; CPU build is enough)
torch>=2.2.0
Pillow>=10.0.0

# TradingView analyst skill
tradingview-ta>=3.3.0

# Chart-drawing scripts only (draw_multi_timeframe_chart.py, vision_draw_*.py)
matplotlib>=3.8.0

# Tests
pytest>=8.0.0
